<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mofong.vvic.dao.CronDao">

	<select id="getCronById" resultType="String" parameterType="String">
		select `cron` from `cron` where
		cron_id=#{id}
	</select>

</mapper>